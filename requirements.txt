# Analysis (CPU only): every table and figure in the dissertation is produced
# from the frozen probe streams with these packages alone.
numpy>=1.26
scikit-learn>=1.5          # TF-IDF and logistic routing classifier (joint tier)
sympy>=1.12                # symbolic answer equivalence, as in DEER's grader
matplotlib>=3.8            # optional: the PNG figures under experiments/ntc

# Generation (GPU only): needed to regenerate probe streams from scratch.
# vllm pulls its own pinned torch build, so install it on the GPU host.
# vllm>=0.6
# transformers>=4.46
# datasets>=3.0

# Development
pytest>=8.0
ruff>=0.6
