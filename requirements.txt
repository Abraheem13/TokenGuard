# TokenGuard — dependencies (Python 3.10–3.13)
# Floor-pinned (>=) so pip resolves a wheel that exists for YOUR Python
# version. We avoid hard "==" pins that lack 3.13 wheels (e.g. pyarrow 17),
# which forced a source build and failed. Reproducibility is preserved via
# the committed pip freeze in docs/ENVIRONMENT.txt after first install.

# Core scientific stack
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
scipy>=1.13
pyarrow>=18.0          # 18.x ships cp313 wheels; 17.x did not

# Deep learning (CPU wheels resolve everywhere; CUDA build on the cluster)
torch>=2.2
transformers>=4.46
sentencepiece>=0.2
peft>=0.13
accelerate>=1.0

# Data / hub
datasets>=3.0
sentence-transformers>=2.7
huggingface_hub>=0.26
filelock>=3.13        # explicit: a broken base-env filelock caused an import crash

# Config / utilities
pyyaml>=6.0
tqdm>=4.66
rich>=13.7

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Product layer (Days 6–7)
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
streamlit>=1.38

# Testing / quality
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6

# Build backend safety (fixes "No module named pkg_resources" on 3.13)
setuptools>=70
wheel>=0.43